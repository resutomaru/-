# Аудит Avito-парсера (audit-first)

Версионируемые артефакты аудита. Частично закрывает **E21**: до этого канон жил только
в Supabase-сниппетах, а git-репо был пуст (один пустой `README.md` + Initial commit).

Мандат (передаточный бриф §0): сначала аудит реестра §2 и переисправление, **только потом** Фазы 5–8.

## Метод — три корзины §2

- **Код-зависимые (E2–E12, E17):** вердикт только по КАНОНУ из Supabase. §3 брифа помечен
  «восстановлено-из-переписки», маппинг §5 — «вероятное, СВЕРИТЬ». Гипотезы формулирую по §3,
  но вердикт «починено/сломано» по пересказанному regex не выношу.
- **Данные-зависимые (E1, working-сторона E18):** прогон запроса по 904 строкам `lots`.
- **Процесс/операционка (E13–E16, E19–E21):** оцениваются по документам.

## Ledger (живой)

| ID | Корзина | Вердикт сейчас | Блокировано на |
|----|---------|----------------|----------------|
| E1  | данные | прочёсаны ВСЕ 98 unknown + 167 working: ~99% чисто, 2 реальных пропуска (БП «перестал включаться»; rx570 «надо шить») закрыты в словаре | повтор после новых данных |
| E2  | код | ✓ **закрыто на наборе** (`dead→working=0`), развёрнуто; на 904 инцидентность ~0 (все dead — это step 1) | — |
| E3  | код | ✓ **закрыто на наборе**, развёрнуто | — |
| E4  | код | ✓ **закрыто на наборе** (override сохраняет E6-хедж), развёрнуто | — |
| E5  | код | ✓ **закрыто на наборе** (override отличает от E6-fan), развёрнуто; остаток→LLM | — |
| E6  | код | закрыто для виденных строк; держим регрессией (контроль) | канон (контроль) |
| E7  | код | **подтверждено по канону** (в git): одиночный gb раньше кита N×M → «2x32gb»→32 | фикс `pk_ram` |
| E8  | код | **подтверждено по канону** (в git): allowlist объёмов без 24/48/96 ГБ | фикс `pk_ram` |
| E9  | код | **фикс написан** (`sql/pk_gpu.sql`): инлайн §3.5 → функция; super = `\ysuper\y` + allowlist Super-линеек | деплой + превью-дифф |
| E10 | код | ✓ **фикс v2.2** (`sql/categorizer.sql`): превью чисто — 6 аксессуаров→other, 0 потерь компонентов | деплой + бэклог |
| E11 | код | открыто, низкий приоритет (Arc/Xeon Scalable) | канон gpu/cpu |
| E12 | код | **подтверждено по канону** (в git): нет валидации объёма ssd | фикс `pk_ssd` |
| E13 | процесс→корректность | поднял: `\y`+кириллица зависит от локали БД; риск в категоризаторе | канон + `lc_ctype` |
| E14 | процесс | открыто; **ПРЕДУСЛОВИЕ** фиксов (E4 — доказательство). Набор стартован (этот коммит) | реальные строки |
| E15 | процесс | открыто; завязан на E14 + реальные строки | экспорт ~300–500 |
| E16 | операционка | разовый разгреб ✓; **расписание собрано** (`sql/normalize_new_lots.sql` + `lot_category()`, pg_cron */2) — новые лоты сами категория→condition→ключи; деплой pending | деплой pg_cron |
| E17 | код/данные | gpu частично (48 строк); ram — открытый дефект (E7) | канон + данные |
| E18 | данные | working-направление дочёсано НА МАСШТАБЕ (11.06, зонд по живым 893 working): 60+ подозреваемых глазами → 2 реальных класса пропусков (заявленный дефект/«вылетает»; «не определяется») закрыты в ШАГ 1 c защитой негаций (lookbehind «не/без»); +6 real-строк в golden (4 danger). Остальное — легит-негации, гарды держат. **ПРОГНАНО 12:03:** dead→working=0, 24 golden=0, история-синк=0; negation-контроли живы; dead 66→71. **unknown-направление (556) — следующий зонд** | зонд по unknown |
| E19 | заблокировано | до медианы и реальных цен (зависит от E20) | оплата rest-app |
| E20 | процесс | открыто, **НЕДООЦЕНЕНО**; незакрытый STOP-гейт; гейтит Ф5–8 | тариф/лимит rest-app |
| E21 | операционка | **ЗАКРЫТО** — приложение Claude было suspended; unsuspend → push работает, аудит-файлы в репозитории | — |

## Артефакты

- `../sql/lot_condition.sql` — **ИСПРАВЛЕННАЯ** функция (канон + step 5 override). 14/14 на наборе, `dead→working=0`; ждёт валидации на 904.
- `condition/lot_condition.canon.sql` — развёрнутый канон (для диффа). Сверено: = §3.12 дословно.
- `condition/golden_set.sql` — таблица `condition_golden` + регрессионный сид (E2–E6) + харнесс
  (матрица ошибок, нулевая терпимость `dead→working` E18, дамп несовпадений).
- `condition/labeling_guide.md` — правила разметки истины, опасная ось, тензия E5↔E6.
- `keys/pk_gpu.golden.sql` — `gpu_key_golden` + harness (23 кейса; E9/v2/Arc). Норма mismatches=0.
- `keys/key_golden.sql` — `key_golden` + регресс-сид cpu/ram/mobo/ssd/psu + harness (диспатч как прод;
  danger=E17/E7/E8 — отравление бакета; gap=NEW-2 vocab). Закрывает E14 для не-gpu ключей.
- `category/category_golden.sql` — `category_golden` + регресс-сид (E10/v2.3 + позитивы) + harness +
  gap-бэклог NEW-1 (аксессуар→компонент). Закрывает E14 для категорий.
- `reaudit-2026-06-08.md` — **ре-аудит приёмника**: перепроверка §2 по живым данным/сниппетам, переисправление.

## Ре-аудит приёмника (2026-06-08) — дельта к леджеру

Полные выкладки: `reaudit-2026-06-08.md`. Источник истины — git + живые выгрузки Supabase, не пересказ §3.

| ID | Было | Стало после ре-аудита |
|----|------|------------------------|
| E7  | подтв. по канону | **подтв. в ЖИВЫХ данных** (`2x4gb`→4, `8x2gb`→2); **фикс написан** (кит-первым + allowlist), не деплоен |
| E8  | подтв. по канону | реален, **латентен** на корпусе; **фикс написан** (allowlist 24/48/96 + тоталы) |
| E9  | гипотеза, латентно | **pk_gpu собран как функция** (не инлайн): super = `\ysuper\y`+allowlist, +добор R9/GTS, +golden; НЕ деплоено (ждёт превью) |
| E10/NEW-1 | v2.2 для виденного | **categorizer v2.3 написан** — 6 протечек (microSD/xpg-БП/мак/водянка-GT360/райзер/Simagic) + recall «80+»; ПЕРЕ-превью перед деплоем |
| E12 | подтв. по канону | реален, **severity НИЗКАЯ** (подтв. данными); **фикс написан** (sanity-диапазон, не allowlist) |
| E17 | gpu частично (48) | **gpu key-level в осн. ЗДОРОВ** (варианты/объём разделяются); остаток — model↔title (`rx570`→`rx580`), низкая частота |
| E16 | разово закрыто | **ЖИВ на масштабе**: ~160 строк `item_category=null`; расписания нет |
| NEW-3 | вероятно дыры | **замерено: ~72% компонентов без position_key** (≈626 строк); блокер Ф5 |
| NEW-4 | риск отката | pk_ssd live==git ДОСЛОВНО; pk_ram/categorizer == git ПО ПОВЕДЕНИЮ; condition/pk_ram дословно не сняты |

**Переисправлено в git (НЕ деплоено):** `sql/pk_ram.sql`, `sql/pk_ssd.sql`, `sql/categorizer.sql`, `sql/pk_gpu.sql` (+ `audit/keys/pk_gpu.golden.sql`).
**Гейты закрытия:** ~~исходник gpu-инлайна~~ → собран `pk_gpu` функцией (E9 + реран gpu-ключей); дословные живые тела pk_ram/lot_condition; re-preview v2.3; missed_dead_probe на 410 (E1/E18); реран position_key (NEW-3).

## Дельта приёмника-2 (2026-06-10) — gpu-ключи
- **`pk_gpu` собран как ФУНКЦИЯ** из реальной выгрузки (266 строк), не воскрешение Supabase-инлайна → канон в git.
- **E9 закрыт по существу:** `super` = слово целиком + номер из Super-линейки; гипотеза брифа `[0-9] ?super` подтверждена негодной.
- **Добор покрытия из нулей выгрузки:** AMD R5/R7/R9, nVidia GTS, отдельный SKU 4070 Ti Super, guard объёма.
- **golden на ключи заведён** (`audit/keys/pk_gpu.golden.sql`) — закрывает E14 для gpu-ключей.
- **Превью-дифф на живых данных: changed=0, emptied=0** (377 gpu-строк); покрытие ~20%→~94% (355/377), 22 null проверены глазами.
- **v2 (по аудиту 22 непокрытых gpu-нулей):** + base-фолбэк на заголовок (§4.4; ловит «GeForce 1050 Ti»→gtx1050ti и «gt210», где model без префикса) с гардом сборок; + ветка **Intel Arc** (E11); golden → 23 кейса. Покрытие ~94%→~96%; остаток (GeForce 210 совсем без префикса, ретро, майнинг CMP) — намеренный safe-null (диапазон-догадка номер→семья небезопасна).
- **NEW-1 (найдено при аудите gpu-нулей; в копилку, НЕ фикшено):** «Radeon DDR4 … MHz»→gpu (это ОЗУ), «Комплект i5 + мат.плата + RX 590»→gpu (это сборка). К целевому проходу по чистоте категорий.
- НЕ деплоено: гейт = golden mismatches=0 + превью-дифф changed/emptied=0; затем реран (NEW-3 для gpu).

## Дельта приёмника-3 (2026-06-11) — сети для ключей и категорий + накопление цен
- **Накопление истории цен** (`sql/snapshot_price_history.sql`, pg_cron */5) + бэкафилл; датчик
  `sql/bucket_readiness.sql` (ETA до K>20). price_history была пуста и не наполнялась — теперь банкуется.
- **E14 закрыт для всех трёх «мозгов»:** к condition (`condition_golden`) и gpu-ключам (`gpu_key_golden`)
  добавлены **`key_golden`** (cpu/ram/mobo/ssd/psu) и **`category_golden`**. Это снимает дыру RR-12/RR-03
  «golden для ключей/категорий — нет» и делает БЕЗОПАСНЫМ расширение словарей (требование NEW-2).
- **expected выведены из самой логики функций** (E7-кит, E8-объёмы DDR5, E12-диапазон, xeon-ревизии,
  so-dimm, bar-число psu). Регресс-сид должен давать mismatches=0 — это и подтверждает «live == git».
- **Новые vocab-дыры вскрыты выводом expected (занесены как gap, НЕ фикшено):** «Pentium **Gold** G7400»→null
  (инфикс «Gold» ломает парс); аксессуары без гарда → компонент («кронштейн/термопрокладки/бэкплейт
  видеокарты»→gpu, «рамка сокета»→mobo); «Crucial MX500 500GB» без слова ssd→other. Это стартовый бэклог NEW-1/NEW-2.
- Гейт прежний: прогнать harness в Supabase → `regression_mismatches=0` подтверждает деплой=канон;
  `gap`-строки = список на «расширить до максимума». Затем — ручная разметка (source='real') и дочёс working (E18).

## Дельта приёмника-4 (2026-06-11, вечер) — САМОАУДИТ дневных фиксов
Перепроверка всего написанного за день свежим взглядом. Находки (закрываем по очереди, D2 → D1 → D3 → D4 → D5/D7 → зонд A):
- **ПРОЦЕСС-0 (признано):** Supabase SQL Editor показывает только ПОСЛЕДНИЙ select блока → встроенные
  «норма: 0 строк» проверки кусков не были видны глазами. Новый формат: все экзамены = одна финальная
  union-таблица. Прошлые куски подлежат пере-верификации консолидированным прогоном.
- **D1 🔴 pk_mobo (сегодняшний фикс):** голые числа-чипсеты (770/870/970/945/965/975) и x58/x48/x38 без
  границ → '970' внутри «i7-9700K»/«GTX 970», 'x58' внутри «RX580» → ядовитый ключ. Фикс: голые числа
  УБРАТЬ (null безопаснее, RR-03), x58/x48/x38 + (?![0-9]).
- **D2 🔴 pk_cpu (сегодняшний фикс):** при сплите Xeon-веток потерян v-суффикс у голых Xeon
  («Xeon 2680 v2»→'xeon-2680', было 'xeon-2680v2') → смешение поколений + раскол с историей. ЗАКРЫТО
  этим коммитом (v-захват возвращён + danger-golden). После деплоя: полный ре-ключ cpu + синк price_history.
- **D3 🟠 pk_ssd/pk_psu:** цена в заголовке («SSD за 500р»→500gb; «БП 350р»→350w) — нужен валютный негатив.
- **D4 🟠 snapshot_price_history:** on conflict do nothing → история не самолечится при пере-ключевании;
  фикс: do update (синк ключа/категории/condition, price/observed_at не трогаем).
- **D5 🟡 lot_category v2.5:** явная mobo-ветка потеряла исключение водоблок/СЖО (латентно, в корпусе 0);
  вернуть узкий гард (кулер не возвращать — это был over-exclude).
- **D7 🟡 lot_is_component:** «плата + RX580» не флагуется сборкой (нет rx/rtx/gtx/видеокарт в +-альтернации).
- D6 = зонд A (gpu-аксессуары не-в-начале строки) — известный precision-остаток, по реальным данным.
- **ЗАКРЫТО (вечер 11.06, один коммит):** D1 (голые числа-чипсеты убраны, x38/48/58+(?![0-9]); D945→
  осознанный null), D3 (валютный негатив ssd/psu: р/руб/₽/rub/шт), D4 (snapshot v2: do update-самосинк +
  самочистка выпавших — ручные синки больше не нужны), D5 (v2.6: узкий гард водоблок/СЖО на явной mobo-ветке),
  D7 (+gpu-слова в детектор сборок). Golden: +7 строк (4 danger). D2 закрыт ранее (вердикт 0/0, v-ключей 16).
  Открыто из самоаудита: только D6 (зонд A). Вердикт-формат: одна union-таблица (процесс-0).
- **D6 ЗАКРЫТ (зонд A на живом корпусе: 7 подозреваемых / ~870 компонентов):** 3 реальных класса протечек →
  v2.7 ^-гард += охлажден|систем*охлажден|адаптер|вентилятор (golden +5, danger ×4). Бандлы в cpu/mobo
  («Комплект Z790+i5») НАМЕРЕННО не перекатегоризуем — гейт is_component=false уже бережёт медиану.
  САМОАУДИТ ПОЛНОСТЬЮ ЗАКРЫТ (D1–D8); финал — консолидированный экзамен всех 4 сетей одной таблицей.
- **D2-fix2 (КОНСОЛИД. ЭКЗАМЕН поймал регресс от D2):** генерик/E-W-L Xeon-ветки имели `v?` (буква v
  необязательна) → число ядра «Xeon X3460 4 ядра»→`xeon-x3460v4`, «E5-2660 8 ядер»→`...v8`. Фикс:
  `(?:\s*v\s*([0-9]))?` — ревизия только при литеральной 'v'. +2 danger-golden. Подтверждение ценности
  ПОСТОЯННОЙ сети: инлайн-чек D2 («X3460 LGA1156», без числа ядра) проглядел, golden из реальной выгрузки — нет.
  Также: финальный экзамен требует пересева gpu_key_golden (`pk_gpu.golden.sql`) — отдельный файл.
- **✅ КОНСОЛИДИРОВАННЫЙ ЭКЗАМЕН ПРОЙДЕН (11.06, ~22:00, на ЖИВЫХ функциях):** все 5 сетей (condition,
  категории, ключи pk_*, gpu-ключи, детектор сборок) + инварианты истории — 10 проверок = 0; gap-бэклог = 2
  (MX500 SSD-словарь + дробные ТБ — осознанно); наблюдений 199. Самоаудит D1–D8 + D2-fix2 закрыт и
  ПОДТВЕРЖДЁН прогоном. Оговорка честности: golden ловит лишь закодированные классы — гарантия растёт по мере
  добавления source='real' строк; открытый roadmap (НЕ ошибки): дочёс working E18, каркас медианы Ф5.
- **D8 (найден при написании вердикта, ЗАКРЫТ):** «комплект» — три смысла, детектор знал один. RAM-киты
  («2x8GB комплект», «2+2 Гб») и «полный комплект» (коробка) флаговались бандлом → терялись из медианы.
  Фикс: вырезаем «полный комплект»; чистый RAM-кит (без второго класса/'+') не триггерит. +component_golden
  (11 кейсов) — у детектора сборок появилась своя сеть (RR-12). Вердикт прогона D1–D7: 7×0 ✓; mobo
  без-ключа 26→29 = снятые ядовитые ключи D1 (ожидаемо).

## Ре-аудит приёмника-3 (2026-06-11, перенос чата) — СТАТИЧЕСКИЙ проход нового чата
Полные выкладки: `reaudit-2026-06-11-static.md` (R0–R10). Построчный проход всего sql/ + сетей
свежими глазами по мандату «полностью перепроверь»; живая БД НЕ затрагивалась.
- **R0 ЗАКРЫТ этим коммитом:** консолидированный экзамен жил ТОЛЬКО в чате (класс E21) →
  восстановлен в git: `consolidated_exam.sql` (один select: 5 сетей + инварианты истории/медиан + info-счётчики).
- Главное из открытого: **R5 ⚠️** gen-only ram-ключ (`'ddr4'` без объёма) копится в price_history →
  будущий бакет-яд для медианы (нужно решение: cap=null → ключ null) — до Ф6; **R1** мёртвые `argb|rgb`
  в гарде ШАГ 2 (де-гомоглиф `b→в` делает латинский паттерн недостижимым); **R4** «775 руб»→lga775
  (класс D3 для mobo); **R3** «МГц»-кириллица не парсится как частота (раскол DDR5-бакетов);
  **R7** qty-лоты («2 x Xeon», «10 шт») не флагуются — нужен зонд. Остальное low/заметки.
- **Чисто:** harness-диспатч == прод; expected сетей == логика функций; D1–D8 реально в коде;
  медиана/снапшот/нормализатор/датчик — инварианты корректны; тихого дрейфа план→реализация нет.
- **Гейты:** (1) live==git дословно (prosrc-CSV) ДО любых фиксов → (2) экзамен на живой БД →
  (3) здоровье pg_cron → (4) пакет R-фиксов с сетями/превью → (5) зонды данных (R7, unknown-556, категории).
- **ПРОГНАНО 11.06 17:28 (Алексей): консолидированный экзамен на живой БД — 14×0 ✓** (5 сетей + инварианты
  истории и медиан), gap-бэклог = 2 (ожидаемые MX500/дробные ТБ). Info-счётчики: наблюдений 259 (рост,
  конвейер живой), медиан 0 (зреют), компонентов без ключа 173 (словарные нули). **R5 подтверждён данными:
  20 gen-only ram-лотов, 5 наблюдений уже в price_history** — копится; закрывать в ближайшем пакете фиксов,
  пока бакет ddr* не дозрел до порога 8. Гейт №2 закрыт; для фиксов ждём гейт №1 (дословный prosrc-дифф).
- **R5 ОДОБРЕН и РАСШИРЕН на все категории (Алексей, 11.06 вечер: «все категории должны быть ограничены
  от такого риска»).** Заведён реестр слабых ключей `keys/weak_keys.md` — полный проход 6 категорий.
  Ещё две яд-формы найдены и зафиксированы В GIT (НЕ деплоено — гейт №1 в силе): `pk_ram` — объём-без-
  поколения ('16gb…', смесь DDR2–DDR5) → теперь gen И cap обязательны; `pk_cpu` — athlon-семейства
  ('athlon-ii'/'x4'/'64') → ключ только по номеру модели. key_golden +7 строк R5; экзамен — блок
  weak-keys 80–83 (до деплоя ожидаемо >0). Решенческие 🟠 (gpu-без-объёма, ssd-без-бренда/интерфейса) —
  зонды `keys/weak_keys_probe.sql`; намеренные 🟡 §4.2 (mobo-сокет, psu-ватты) кодом НЕ переоткрыты —
  решение заказчика после зонда №3. Деплой-процедура пакета — в шапке `sql/pk_ram.sql`.
- **ГЕЙТЫ №1 и №3 ЗАКРЫТЫ (11.06 ~18:00, выгрузки Алексея).** Prosrc-дифф всех 12 живых функций против
  git-канона: **логика идентична 1:1** — отличия только комментарии/форматирование (деплой шёл без
  внутренних комментов), единственная содержательная дельта = два ещё НЕ деплоенных R5-фикса
  (pk_ram weak-keys, pk_cpu athlon) — ожидаемо. Скрытых расхождений НЕТ; NEW-4 снят полностью.
  pg_cron: 3/3 джоба active (normalize */2, snapshot */5, medians '3 * * * *'), последние 20 запусков —
  все succeeded, тихих падений нет. PostgreSQL 17.6 (lookbehind/`\y` ок — E13-контекст закрыт).
  **Дана отмашка на деплой R5-пакета**; после деплоя экзамен: weak-keys 80–83 = 0.
- **R5-ПАКЕТ ЗАДЕПЛОЕН И ПОДТВЕРЖДЁН (11.06 18:09, прогон Алексея):** pk_ram+pk_cpu заменены, ram/cpu
  пере-ключёваны, key_golden пересеян (единственный mismatch = gap decimal-tb — ожидаемо), новый экзамен —
  **18×0 ✓** включая weak-keys 80–83 (gen-only в lots 20→0; в history 5→0 — самочистка D4 отработала сама).
  Безключевых компонентов 173→211: слабые формы честно ушли в null (20 gen-only + объём-без-поколения +
  athlon-семейства; вторую форму до деплоя не замеряли, отсюда +38). Наблюдений 270 и растёт.
  **Слой «ключи» ре-аудита ЗАКРЫТ.** Остаток: зонды №1–2 (gpu-объём, ssd-бренд/интерфейс), №3 — после
  созревания history; R1–R4/R6/R8/R10 — следующий пакет (некритичные, словарные).
- **ПАКЕТ №2 СОБРАН В GIT (11.06, вечер; НЕ деплоен):** R1 (rgb-гард после де-гомоглифа: a?rg[bв]),
  R2 (нормализация пробелов в lot_condition — лукбихайды), R3 (pk_ram: МГц/МТ/с кириллицей + DDR5
  6800–8400), R4 (pk_mobo: «775 руб» ≠ сокет), R6 (pk_gpu: бандл-гард на primary-пути), R8
  (is_component: кит по mhz/hyperx), R10 (lot_category v2.8: [bzh][2-8][0-9]0 + x[3-8]70 + psu-гард —
  «Блок питания B650» больше не mobo), NEW-5 (security_invoker на все 5 eval-вью). Сети: condition +2,
  gpu +1, keys +4, component +1, category +3 кейсов. Деплой: 6 функций → полный пересчёт разметки
  (категория → condition/is_component → ключи) → пересев 4 сетей → экзамен (ожидание: 18×0).
  +зонд №4 (R7 qty-лоты) в `keys/weak_keys_probe.sql`.

- **ПРОГНАНО 11.06 (Алексей):** keys — единственный mismatch = `gap` Pentium Gold (44 регресса зелёные,
  E7/E8/E12 подтверждены на ЖИВОЙ функции → **прод == git-канон**); category — 4 протечки = ровно
  `gap`-аксессуары, регресс зелёный. Сеть и вывод `expected` валидны.
- **ЗАКРЫТО v2.4 (11.06):** `lot_category` +^-гард аксессуаров (кронштейн/держатель/бэкплейт/рамка/наклейка/
  термопрокладки/термопаста); `pk_cpu` +инфикс gold/silver; `categorizer.sql` → тонкая обёртка над `lot_category`
  (убрана 2-я копия правил, NEW-4). 5 gap→regression в golden. MX500 (словарь SSD-моделей) — остаётся `gap`,
  на реальную выборку. Деплой: CREATE OR REPLACE обеих функций → превью-дифф → реран категоризатора → чистка
  position_key/price_history у выпавших не-компонентов → повтор harness (ждём regression=0, дыр −5).
